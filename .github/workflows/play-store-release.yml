name: Play Store Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version'
        required: true

jobs:
  security-rules-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java (Firebase Emulator)
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '21'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: firebase/rules-testing/package-lock.json

      - name: Install rules-testing dependencies
        working-directory: firebase/rules-testing
        run: npm ci

      - name: Run Firestore/Storage security-rules suite (Firebase Emulator)
        working-directory: firebase/rules-testing
        run: npm test

  build-android:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.3'
          cache: true

      - name: Get dependencies
        run: flutter pub get

      - name: Decode google-services.json
        env:
          GS_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          if [ -n "$GS_JSON" ]; then
            echo "$GS_JSON" > android/app/google-services.json
            echo "Wrote google-services.json from secret."
          else
            echo "GOOGLE_SERVICES_JSON secret not set; using committed google-services.json."
          fi

      - name: Ensure debug keystore
        run: |
          if [ ! -f "$HOME/.android/debug.keystore" ]; then
            mkdir -p "$HOME/.android"
            keytool -genkeypair -v \
              -keystore "$HOME/.android/debug.keystore" \
              -storepass android -alias androiddebugkey -keypass android \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Android Debug,O=Android,C=US" >/dev/null 2>&1
          fi

      - name: Configure release signing
        env:
          KEYSTORE_B64: ${{ secrets.RELEASE_KEYSTORE_B64 }}
          STORE_PASS: ${{ secrets.RELEASE_STORE_PASSWORD }}
          KEY_PASS: ${{ secrets.RELEASE_KEY_PASSWORD }}
          KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
        run: |
          if [ -n "$KEYSTORE_B64" ]; then
            echo "$KEYSTORE_B64" | base64 -d > android/my-release-key.jks
            printf 'storePassword=%s\nkeyPassword=%s\nkeyAlias=%s\nstoreFile=my-release-key.jks\n' \
              "$STORE_PASS" "$KEY_PASS" "$KEY_ALIAS" > android/key.properties
            echo "Release signing configured."
          else
            echo "RELEASE_KEYSTORE_B64 not set; falling back to debug signing."
          fi

      - name: Build Android Release Bundle
        run: |
          flutter build appbundle \
            --release \
            --build-number=${{ github.run_number }}

      - name: Build Android Release APK
        run: |
          flutter build apk \
            --release \
            --build-number=${{ github.run_number }}

      - name: Upload Android Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-release
          path: |
            build/app/outputs/bundle/release/app-release.aab
            build/app/outputs/flutter-apk/app-release.apk

      # Play upload needs a Play service account secret AND an upload key that
      # matches the Play Console app. CI has no keystore, so the AAB is
      # debug-signed and this step may legitimately fail — the Android
      # artifacts above are the reliable deliverable. Never fail the build.
      - name: Upload to Google Play
        continue-on-error: true
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_STORE_SERVICE_ACCOUNT }}
          packageName: com.radhika.app
          releaseFiles: build/app/outputs/bundle/release/app-release.aab
          track: internal
          status: completed
